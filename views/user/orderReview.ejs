<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Review Your Order | ModMen</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

  <style>
    body {
      background:#f8f8f8;
      font-family:'Bitter', serif;
    }
    .main-box {
      background:#fff;
      border-radius:20px;
      padding:28px;
      box-shadow:0 6px 18px rgba(0,0,0,0.06);
    }
    .summary-card {
      background:#fff;
      border-radius:16px;
      padding:20px;
      border:1px solid #eee;
    }
    .product-row {
      display:flex;
      align-items:center;
      gap:12px;
      padding:14px 0;
      border-bottom:1px dashed #ddd;
    }
    .product-row img {
      width:70px;
      height:70px;
      object-fit:cover;
      border-radius:8px;
    }
    .total-box {
      background:#f2f6ff;
      padding:18px;
      border-radius:12px;
      border:1px solid #d9e6ff;
    }
    .place-btn {
      background:#000;
      padding:12px;
      border:none;
      color:#fff;
      width:100%;
      border-radius:10px;
      font-weight:600;
    }
    .cancel-btn {
      width:100%;
      padding:12px;
      border-radius:10px;
      border:1px solid #000;
      background:#fff;
    }
  </style>

</head>
<body>

  <%- include('../partials/user/header') %>

  <div class="container my-4">

    <div class="main-box">

      <h4 class="fw-bold mb-4 text-center">Review Your Order</h4>

      <div class="row g-4">

        <div class="col-lg-6">
          <div class="summary-card">

            <h5 class="fw-bold mb-3">Delivery Address</h5>

            <p class="mb-1 fw-semibold"><%= address.fullName %></p>
            <p class="mb-1 small"><%= address.houseNo %>, <%= address.city %></p>
            <p class="mb-1 small"><%= address.district %>, <%= address.state %> - <%= address.pincode %></p>
            <p class="small text-muted">Phone: <%= address.phone %></p>

            <hr class="my-4">


            <h5 class="fw-bold mb-3">Payment Method</h5>
<p class="mb-0 fw-semibold">

  <% const payMethod = typeof paymentMethod !== "undefined" ? paymentMethod : "cod"; %>

  <% if (payMethod === 'online') { %>
    Online Payment (Razorpay)
  <% } else if (payMethod === 'wallet') { %>
    Wallet Payment
  <% } else { %>
    Cash on Delivery
  <% } %>

</p>



          </div>
        </div>

        <div class="col-lg-6">
          <div class="summary-card">

            <h5 class="fw-bold mb-3">Order Items • <%= items.length %> products</h5>

            <% items.forEach(item => { %>
              <div class="product-row">
                <img src="<%= item.productId.images?.[0]?.url || '/placeholder.png' %>">

                <div style="flex:1;">
                  <div class="fw-semibold"><%= item.productId.name %></div>
                  <div class="small text-muted">Size: <%= item.size %> | Color: <%= item.color %></div>
                  <div class="small text-muted">Qty: <%= item.quantity %></div>
                </div>

                <div class="text-end">
                  <div class="text-muted text-decoration-line-through">₹<%= item.variantPrice %></div>
                  <div class="fw-bold">₹<%= item.salePrice * item.quantity %></div>
                </div>
              </div>
            <% }) %>

          </div>
        </div>

      </div>

      <div class="row mt-4">
        <div class="col-lg-12">
          <div class="total-box">

            <div class="d-flex justify-content-between mb-2">
              <span>Subtotal</span>
              <span>₹<%= subtotal.toFixed(2) %></span>
            </div>

            <div class="d-flex justify-content-between mb-2">
              <span>CGST (9%)</span>
              <span>₹<%= cgst.toFixed(2) %></span>
            </div>

            <div class="d-flex justify-content-between mb-2">
              <span>SGST (9%)</span>
              <span>₹<%= sgst.toFixed(2) %></span>
            </div>

            <div class="d-flex justify-content-between mb-2">
              <span>Shipping Charge</span>
              <span><%= shippingCharge > 0 ? "₹" + shippingCharge : "Free" %></span>
            </div>

            <div class="d-flex justify-content-between mb-2 text-success fw-bold">
              <span>Applicable Discount</span>
              <span>- ₹<%= discount.toFixed(2) %></span>
            </div>

            <hr>

            <div class="d-flex justify-content-between fs-5 fw-bold">
              <span>Total Payable</span>
              <span>₹<%= payableTotal.toFixed(2) %></span>
            </div>

          </div>
        </div>
      </div>

      <div class="row mt-4">
        <div class="col-lg-6">
          <form action="/user/confirmOrder" method="POST">
            <input type="hidden" name="addressId" value="<%= address._id %>">
            <input type="hidden" name="paymentMethod" value="<%= payMethod %>">
            <button type="submit" class="place-btn">Confirm Order</button>
          </form>
        </div>

        <div class="col-lg-6">
          <a href="/user/checkoutPayment?addressId=<%= address._id %>" class="cancel-btn text-center d-block">
            Cancel
          </a>
        </div>
      </div>
    </div>

  </div>

  <%- include('../partials/user/footer') %>

</body>
</html>
