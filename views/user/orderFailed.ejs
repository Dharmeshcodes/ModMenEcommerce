<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f4f6f9;
            font-family: 'Poppins', sans-serif;
        }

        .failed-box {
            max-width: 600px;
            margin: 80px auto;
            background: #fff;
            padding: 40px;
            border-radius: 18px;
            box-shadow: 0px 8px 22px rgba(0,0,0,0.09);
        }

        .failed-img {
            width: 140px;
            margin-bottom: 15px;
            animation: float 2s infinite ease-in-out;
        }

        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-8px); }
            100% { transform: translateY(0px); }
        }

        .btn-theme {
            background-color: #0d6efd;
            color: #fff;
        }

        .btn-theme:hover {
            background-color: #0b5ccc;
            color: white;
        }

        .btn-outline-theme {
            border: 2px solid #0d6efd;
            color: #0d6efd;
        }

        .btn-outline-theme:hover {
            background-color: #0d6efd;
            color: #fff;
        }
    </style>
</head>

<body>

    <div class="failed-box text-center">

        <img src="https://cdn-icons-png.flaticon.com/512/463/463612.png" class="failed-img" alt="Failed">

        <h2 class="fw-bold text-danger mb-2">Payment Failed</h2>

        <p class="text-muted mb-4">
            Unfortunately, your transaction could not be completed.  
            You can try again or check the order details for more information.
        </p>

        <div class="d-flex justify-content-center gap-3 mt-4">

    <button class="btn btn-primary px-4 pay-2 rounded-pill" id="retryPaymentBtn" data-order-id="<%=order.orderId %>"> Retry Payment</button>

    <a href="/user/order/<%= order.orderId %>" 
       class="btn btn-outline-theme px-4 py-2 rounded-pill">
        View Order
    </a>

</div>

        <p class="mt-4 text-muted" style="font-size: 14px;">
            Order ID: <strong><%= order.orderId %></strong>
        </p>

    </div>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
document.getElementById("retryPaymentBtn")?.addEventListener("click", async () => {

    const orderId = document.getElementById("retryPaymentBtn").dataset.orderId;

    const res = await fetch(`/user/retry-payment/${orderId}`, {
        method: "GET",
        headers: { "Content-Type": "application/json" }
    });

    const data = await res.json();

    if (!data.success) {
        return Swal.fire({
            icon: "error",
            title: "Retry Not Allowed",
            text: data.message || "Retry not available for this order"
        });
    }

    window.location.href = data.redirectURL;

});
</script>


</body>
</html>
